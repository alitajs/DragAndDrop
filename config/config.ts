export default {
    // Umi 配置
    doc: {
        // father-doc 独有配置
        title: 'DragAndDrop',
        desc: '一个react的dnd封装，兼容手机端，简化用法。',
        logo: 'https://avatars1.githubusercontent.com/u/49136103?s=200&v=4',
        // include:['docs']
    }
}
// ### doc.include
// - 类型：`Array<String>`
// - 默认值：`['docs']`
// - 详细：
// 配置默认会嗅探的除 `src` 目录以外的目录，该目录下的 `.md` 文件也会被加入进路由，而且优先级高于 `src` 目录。
